{% extends 'base-front.html.twig' %}

{% block title %}Favorite{% endblock %}

{% block body %}
    <h1>Favorite</h1>

    <table class="table">
        <tbody>
        {% if favorite.camping is not null %}

            <tr>
                <th>Nom</th>
                <td>{{ nom }}</td>
            </tr>
            <tr>
                <th>Date de camping</th>
                <td>{{ date|date('Y-m-d')  }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ favorite.createdAt ? favorite.createdAt|date('Y-m-d ') : '' }}</td>
            </tr>
            {% else %}
            <tr>
                <th>Nom</th>
                <td>{{ nom }}</td>
            </tr>
            <tr>
                <th>Date de camping</th>
                <td>{{ date|date('Y-m-d')  }}</td>
            </tr>
            <tr>
                <th>Created At</th>
                <td>{{ favorite.createdAt ? favorite.createdAt|date('Y-m-d ') : '' }}</td>
            </tr>
{% endif %}
        </tbody>
    </table>

    <a href="{{ path('app_favorite_index') }}">back to list</a>


    {{ include('favorite/_delete_form.html.twig') }}
{% endblock %}
