<form method="post" action="{{ path('app_randonnee_delete', {'idRandonnee': randonnee.idRandonnee}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ randonnee.idRandonnee) }}">
    <button class="btn">Delete</button>
</form>
