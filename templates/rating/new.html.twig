{% extends 'base-admin.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
<main id="main" class="main">
		<section class="section">
			<div class="row">
				<div class="col-lg-12">
					<h1>New Rating</h1>

					<div class="col-12">
						<div class="card">
							<div class="card-body">

    {{ include('rating/_form.html.twig') }}
{% if app.request.query.has('flash') %}
    <div class="alert alert-danger">{{ app.request.query.get('flash') }}</div>
{% endif %}
    <a href="{{ path('app_participation_index') }}">back to list</a>
    </div>
					</div>
				</div>
			</div>
		</section>

	</main>
{% endblock %}
