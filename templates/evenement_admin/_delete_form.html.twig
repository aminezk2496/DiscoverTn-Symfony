<form method="post" action="{{ path('app_evenement_admin_delete', {'idEvent': evenement.idEvent}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement.idEvent) }}">
    <button class="btn btn-primary">Delete</button>
</form>
