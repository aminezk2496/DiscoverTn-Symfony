{% extends 'base-admin.html.twig' %}

{% block title %}Nouveau Evenement{% endblock %}

{% block body %}
<main id="main" class="main">
    <section class="section">
        <div class="row">
            <div class="col-lg-12">

                <div class="card">
                    <div
                            class="card-body">
    <h1>Ajouter Evenement</h1>

    {{ include('evenement_admin/_form.html.twig') }}

    <a class="btn btn-primary" role="button" href="{{ path('app_evenement_admin_index') }}">Retour à la liste</a>
                    </div>
                </div>
            </div>
        </div>
    </section>

</main>
{% endblock %}
