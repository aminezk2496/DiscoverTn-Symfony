{% extends 'base-admin.html.twig' %}

{% block title %}Edit Evenement{% endblock %}

{% block body %}
<main id="main" class="main">
		<section class="section">
			<div class="row">
				<div class="col-lg-12">
					<h1>Edit Evenement</h1>

					<div class="col-12">
						<div class="card">
							<div class="card-body">

    {{ include('evenement_admin/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_evenement_admin_index') }}">back to list</a>

    {{ include('evenement_admin/_delete_form.html.twig') }}
</div>
					</div>
				</div>
			</div>
		</section>

	</main>
    
{% endblock %}
